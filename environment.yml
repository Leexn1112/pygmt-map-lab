name: pygmt-workshop
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pygmt=0.17
  - gmt=6.5
  - ghostscript=10.04
  - pandas
  - pillow
  - nbformat
  - nbclient
  - ipykernel
  - ipywidgets
  # 以下只有 scripts/prototype_*.py 需要；三份 Notebook 不用
  - netcdf4
  - scipy
  - requests
